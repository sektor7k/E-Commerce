"use client"

interface KeyCardProps {
    value: string | null;
};

export const KeyCardProps = ({
    value
}: KeyCardProps) => {

    return (
        <div>
            KeyCards
        </div>
    )
}